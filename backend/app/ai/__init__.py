"""app.ai package — AI pipeline modules for task extraction.

Keep imports lazy to avoid triggering configuration/database setup at package
import time. Submodules remain available through normal import mechanics.
"""
